class ImageZoom {
  constructor(imageId, resultId) {
    this.image = document.getElementById(imageId);
    this.result = document.getElementById(resultId);
    // Code
  }
}

new ImageZoom("image", "result");
